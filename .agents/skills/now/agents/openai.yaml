interface:
  display_name: "Now"
  short_description: "Repo-specific patterns and workflows for now"
  default_prompt: "Use the now repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true